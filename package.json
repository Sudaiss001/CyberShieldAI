{
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build && if not exist .next\\standalone\\.next mkdir .next\\standalone\\.next && xcopy /E /I /Y .next\\static .next\\standalone\\.next\\static && xcopy /E /I /Y public .next\\standalone\\public",
    "start": "set NODE_ENV=production&& node .next/standalone/server.js",
    "lint": "eslint .",
    "db:push": "prisma db push --accept-data-loss",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:reset": "prisma migrate reset"
  }
}
